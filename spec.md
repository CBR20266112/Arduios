# 아두이노 교육용 시뮬레이터 앱 - 명세서 (spec.md)

## 1. 개요 (Overview)
본 프로젝트는 초보자가 아두이노(Arduino)의 기본 구조와 회로 연결 흐름을 직관적으로 학습할 수 있는 **교육용 시뮬레이션 웹/모바일 앱**입니다.
단순 회로 계산기가 아니라, **아두이노 실물 키트를 손으로 직접 만지고 조립하는 인터랙션 감각**을 제공하는 데 주안점을 둡니다.

- **현재 대상 Platform**: 웹앱 (HTML5 / Modern JS / Canvas/SVG 기반 웹 애플리케이션)
- **향후 이식 대상**: Android (APK) 및 iOS App Store (Capacitor/WebView/PWA 기반 패키징 대비)
- **주요 UI 언어**: 한국어 (기본), 다국어 확장(영어 등) 대응 구조

---

## 2. 화면 및 유저 플로우 명세

### 2.1. 시작 화면 (Title Screen)
- **배경 & UI**: 깔끔한 엠보싱 3D 느낌의 아두이노 부품 키트 상자 이미지/그래픽 배치.
- **상단 헤더**: 앱 타이틀 ("아두이노 시뮬레이터"), 설정 아이콘(⚙️), 도움말 아이콘(?).
- **중앙 요소**: 실물 투명 키트 상자 그래픽.
- **하단 액션**: `▶ Touch to start` ("화면을 터치하여 시작") 버튼.
- **하단 캡션**: "아두이노 회로 학습 시뮬레이터".

### 2.2. 메인 회로 작업 화면 (Circuit Workspace Screen)
- **상단 영역 (Header & Laptop View)**:
  - 프로젝트 selector ("새 프로젝트 v"), 저장/신규 버튼(💾+).
  - 상단 노트북 그래픽 & status badge (`🟢 USB 연결됨`).
  - 노트북 케이블이 작업 공간으로 시각적 연결.
  - "작업 공간 크기 조절" 드래그 핸들.
- **중앙 영역 (회로 작업 공간 Canvas)**:
  - 다크 톤 그리드(Grid) 배경 pattern.
  - 뷰포트 조작 툴바: 확대(+), 축소(-), 전체 보기([ ]), 현재 배율 (100%).
  - 빈 화면 초기 안내: 점선 상자 + "부품 버튼을 눌러 조립을 시작하세요 / 아직 배치된 부품이 없습니다".
  - 부품 조립 캔버스: 아두이노 UNO, 브레드보드, LED, 저항, 점퍼선, 버튼, 센서 등 배치 및 핀 연결 지원.
  - 부품 터치/길게 누르기(Long Press 2초) 인터랙션: 선택 링과 `집어넣기` (부품 회수) 버튼 노출.
- **하단 영역 (Bottom Navigation & Drawer)**:
  - `[🔄 리셋]` 버튼: 작업 공간 초기화 confirming.
  - `[🧰 부품]` 버튼: 부품 보관함 팝업 열기.

### 2.3. 부품 보관함 팝업 (Components Drawer/Modal)
- **형태 1 (카드 슬라이더 / 그리드)**: 아두이노, 브레드보드, LED, 저항, 점퍼선, 버튼, 센서 아이콘과 `[꺼내기]` 버튼.
- **형태 2 (실물 키트 상자 팝업)**: 키트 상자 내 부품 터치 시 캔버스 배치.

### 2.4. 설정 화면 (Settings Modal)
- **언어**: 앱 언어 (한국어 기본 / English / 日本語 / 언어 추가).
- **화면**: 화면 밝기 슬라이더, 글자 크기, 고대비 모드 토글.
- **조작**: 길게 누르기 시간 (예: 2초), 터치 진동, 조작 확인 안내.
- **회로 작업 공간**: 격자 표시 토글, 부품 자동 정렬, 기본 확대 배율, 작업 공간 기본 크기.
- **팝업 표시**: 팝업 표시 방식 (배경 흐림 blur / 배경 어둡게 / 완전히 불투명하게).
- **도움말 & 정보**: 사용 방법, 아두이노 기초 안내, 앱 정보.
- **초기화**: `[🔄 설정 초기화]` 버튼 및 확인 팝업.

---

## 3. 데이터 및 상태 관리 구조 (Data & State Architecture)

- **View와 Logic 분리**:
  - `AppState`: 현재 화면 (Title / Main / Settings), 다국어 설정, UI 테마 설정.
  - `WorkspaceState`: 배치된 부품 목록 (`components`), wire 연결 정보 (`wires`), zoom/pan 변환 상태.
  - `ComponentData`: 부품 ID, 타겟 타입, position(x, y), rotation, pin 상태.
- **APK / Web 호환성**:
  - `localStorage` 또는 `IndexedDB` 기반 로컬 저장소 추상화 API (향후 Native Plugin 호환).
  - 터치(Touch) 및 마우스(Mouse) 이벤트 수용 Pointer Event 핸들러 설계.
